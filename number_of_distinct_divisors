# Calculate the total number of positive divisors of a positive integer input, including 1 and itself
def number_of_divisors(input_number_here):
    frequency = 0
    
    if input_number_here == 1:
        return 1
    else:
        for test_integer in range(1, int(pow(input_number_here,1/2)) + 1):
            if input_number_here % test_integer == 0:
                frequency += 2
            else:
                pass
        return frequency
        

# Calculate the number of proper divisors         
def number_of_proper_divisors(input_number_here):
    if input_number_here == 1:
        return 0
    else:
        return number_of_divisors(input_number_here) - 2
